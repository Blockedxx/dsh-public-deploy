# 复制为 .env.public 并填入你的公网域名
# 该文件在仓库根目录，restart.sh 会自动加载
DSH_PUBLIC_HOST=abc123.app.workbuddy.host

# 可选：自定义端口（默认 3000 / 13080）
# DSH_PUBLIC_PORT=3000
# DSH_INNER_PORT=13080

# 可选：自定义 profile 目录（默认 /root/.dsh/profiles/web）
# DSH_PROFILE_DIR=/root/.dsh/profiles/web
